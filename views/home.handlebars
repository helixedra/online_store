<div class="wrapper container container-sm container-md container-lg container-xl">
    {{> header }}
    <main>
        <section>
            <div id="carouselExampleCaptions" class="carousel slide" data-ride="carousel">
                {{!-- <ol class="carousel-indicators">
                        <li data-target="#carouselExampleCaptions" data-slide-to="0" class="active"></li>
                        <li data-target="#carouselExampleCaptions" data-slide-to="1"></li>
                        <li data-target="#carouselExampleCaptions" data-slide-to="2"></li>
                    </ol> --}}
                <div class="carousel-inner">

                    {{#each slider}}
                        <div class="carousel-item {{this.s_option}}">
                            <img src="images/{{this.image}}" class="d-block w-100" alt="{{this.title}}">
                            <div class="carousel-caption d-none d-md-block">
                                <h1 class="slider-title">{{this.title}}</h1>
                                <p class="slider-desc d-none d-lg-block">{{this.description}}</p>
                                <a href="{{this.link}}" class="action-btn">Подробнее</a>
                            </div>
                        </div>
                    {{/each}}



                    {{!-- <div class="carousel-item active">
                        <img src="images/lansot-cover-image-01.jpg" class="d-block w-100" alt="...">
                        <div class="carousel-caption d-none d-md-block">
                            <h1 class="slider-title">Мебель и декор в <br>минималистичном стиле</h1>
                            <p class="slider-desc d-none d-lg-block">Проснувшись однажды утром после беспокойного сна,
                                <br>Грегор
                                Замза обнаружил, что он у себя в постели <br>превратился в страшное насекомое.</p>
                            <a href="#" class="action-btn">Подробнее</a>
                        </div>
                    </div> --}}

                    {{!-- <div class="carousel-item">
                        <img src="images/lansot-cover-image-01.jpg" class="d-block w-100" alt="...">
                        <div class="carousel-caption d-none d-md-block">
                            <h1 class="slider-title">Мебель и декор в <br>минималистичном стиле</h1>
                            <p class="slider-desc d-none d-lg-block">Проснувшись однажды утром после беспокойного сна,
                                <br>Грегор
                                Замза обнаружил, что он у себя в постели <br>превратился в страшное насекомое.</p>
                            <a href="#" class="action-btn">Подробнее</a>
                        </div>
                    </div> --}}

                </div>
                {{!-- <a class="carousel-control-prev d-none d-lg-block" href="#carouselExampleCaptions" role="button" data-slide="prev">
                    <i class="far fa-long-arrow-left"></i>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next d-none d-lg-block" href="#carouselExampleCaptions" role="button" data-slide="next">
                    <i class="far fa-long-arrow-right"></i>
                    <span class="sr-only">Next</span>
                </a> --}}
            </div>
        </section>
        <section>
            <h3 class="section-title">Популярные товары</h3>
            <div class="top-products row row-cols-1 row-cols-md-2 row-cols-lg-4 products-container">

                {{!-- {{#each products}}
{{#each this}}
{{uri}}
{{/each}}
{{/each}} --}}

                {{!-- {{log products.id}} --}}

                {{#each products}}
                    <div class="product-tile">
                        <a href="/products/p/{{this.id}}?ref={{this.ref}}&color={{this.color}}&size={{this.size}}">
                            <img src="/images/products/{{this.uri}}/{{this.cover_img}}" class="product-img"
                                alt="{{this.title}}">
                            <div class="product-title">{{this.title}}</div>
                        </a>
                        <div class="price">
                            <div class="prev-price {{#if this.old_price}}{{else}}hidden{{/if}}">{{this.old_price}}</div>
                            <div class="current-price">{{this.price}}</div>
                        </div>
                        <div class="product-status">
                            {{#if this.stock}}
                                <span>Есть в наличии</span>
                            {{else}}
                                <span class="red">Нет в наличии</span>
                            {{/if}}
                        </div>
                        {{!-- <button type="button" class="add-to-cart-s" data-id="{{this.id}}" {{#if this.stock}}{{else}}disabled{{/if}}>
                        <i class="far fa-shopping-cart"></i>
                    </button> --}}
                    </div>
                {{/each}}

            </div>
        </section>
        {{!-- <section>
            <h3 class="section-title">Новости</h3>
            <div class="top-news row row-cols-1 row-cols-md-2 row-cols-lg-4 products-container">
                {{#if news}}
                {{#each news}}
                <div class="news-tile">
                    <div class="news-date">{{this.date}}</div>
                    <div class="news-title"><a href="/news/{{this.id}}?ref={{this.ref}}">{{this.title}}</a>
                    </div>
                    <div class="news-descr">{{this.preview}}</div>
                </div>
               {{/each}}
               {{else}}
               <p>Нет новостей<p>
               {{/if}}
            </div>
        </section> --}}
    </main>

</div>

{{> footer }}